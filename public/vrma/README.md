# モーション（VRMA）の置き場所

```
public/vrma/<motionId>.vrma
```

Mixamoの人型モーション（FBX）をBlenderで**オフラインで**VRMAへ変換して置きます。
ブラウザ実行時にFBXを変換することはしません。手順は `docs/motion-pipeline.md`
（Phase 3で作成）にまとめます。

まだ1本も置かれていません（Phase 0時点）。既定モーションIDは `idle` です。
