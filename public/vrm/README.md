# VRM本体の置き場所

```
public/vrm/<personaId>/<variantId>.vrm
public/vrm/<personaId>/<variantId>.poster.webp   （任意。VRM読込前後に出す静止画）
```

`personaId` は `aimi` / `shizuku` / `nagi` / `hinata` / `rena` のいずれか。
置いてから `npm run dev` を立て直すか `npm run assets` を実行すると、
クローゼットの衣装タブに反映されます。

まだ1体も置かれていません（Phase 0時点）。
